# =============================================================================
# VibeCampus 仓库根 .gitignore
# 说明：子目录另有 .gitignore（如 VibeCampus-Backend、VibeCampus-Frontend）；
# 此处补充「跨模块 / 根路径」且不应入库的内容。
# =============================================================================

# ----- Obsidian（个人工作区 UI 状态，无需版本化）-----
.obsidian/workspace.json

# ----- JetBrains IDEA / IntelliJ（本机数据源、Apifox 等插件缓存、路径；勿提交）-----
# 注意：VibeCampus-Backend/.gitignore 中的 .idea 仅作用于后端子目录，不会忽略仓库根下的 .idea/
.idea/

# ----- IntelliJ 若在仓库根生成的模块描述文件 -----
*.iml
*.ipr
*.iws

# ----- 操作系统杂项 -----
.DS_Store
Thumbs.db

# ----- 日志与编辑器临时文件 -----
*.log
*.tmp
*.swp
*~

# ----- 本地环境变量覆盖（可能含 API Key；示例文件仍用 *.example 提交）-----
.env.local
.env.*.local

# ----- 常见构建产物（与子项目规则并存，避免在根误 add）-----
**/target/
**/node_modules/
**/dist/
