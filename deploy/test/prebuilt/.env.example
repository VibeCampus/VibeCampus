# 与 deploy/test/.env.example 相同含义；复制为 .env 后修改密码
# cp .env.example .env

MYSQL_ROOT_PASSWORD=请改为强密码
MYSQL_DATABASE=vibecampus
MYSQL_USER=vibecampus
MYSQL_PASSWORD=请改为强密码

WEB_PORT=80

# 可选：仅上传 prebuilt + 制品、未克隆整仓时，将 schema.mysql.sql 放在本目录并设为：
# MYSQL_INIT_SQL=./schema.mysql.sql
