# 复制本文件为 application-local.yml 后再填写真实连接信息。
# application-local.yml 已被 .gitignore 忽略，勿将密码提交到仓库。
#
# cp application-local.example.yml application-local.yml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vibecampus?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: YOUR_PASSWORD_HERE
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 本地启用 Redis 时取消注释并修改
  # data:
  #   redis:
  #     host: localhost
  #     port: 6379
  #     password:
